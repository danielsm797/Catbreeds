import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'co.com.catd.breeds',
  appName: 'catsbreeds',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
